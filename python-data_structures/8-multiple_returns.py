#!/usr/bin/python3

# return a tuple with the length of a string and its first character
def multiple_returns(sentence):
    if (sentence == ""):
        sentence[0] == ""

    length = len(sentence)
    first = sentence[0]

    return length, first
